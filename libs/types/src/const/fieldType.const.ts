export enum EFieldType {
	NUMBER = 'number',
	MONEY = 'money',
	DATE = 'date',
	BOOLEAN = 'boolean',
	TEXTFIELD = 'textfield',
	COLORPICKER = 'colorpicker',
	PHONE = 'phone',
	SELECT = 'select',
	CARD = 'card',
	CUSTOM = 'CUSTOM',
}
